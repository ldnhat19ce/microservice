server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  instance:
    preferIpAddress: true

spring:
  application:
    name: eureka-server
  zipkin:
    base-url: http://localhost:9411